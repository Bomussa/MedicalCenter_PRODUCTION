let LANG='ar'; let VOICE=true; let SESSION_ID=null;
function t(ar,en){ return LANG==='ar'?ar:en; }
function speak(msg){ try{ if(!VOICE) return; const u=new SpeechSynthesisUtterance(msg); u.lang=LANG==='ar'?'ar-SA':'en-US'; speechSynthesis.cancel(); speechSynthesis.speak(u);}catch{} }
function toggleLang(){ LANG=(LANG==='ar'?'en':'ar'); document.getElementById('langFlag').textContent=LANG.toUpperCase(); document.getElementById('notice').textContent=t('تم تغيير اللغة إلى العربية.','Language set to English.'); }
function toggleVoice(){ VOICE=!VOICE; alert(VOICE? t('تم تشغيل الصوت','Voice ON'): t('تم إيقاف الصوت','Voice OFF')); }
async function startExam(){ const id=document.getElementById('idnum').value.trim(); const gender=document.getElementById('gender').value; const exam=document.getElementById('exam').value;
  const res=await fetch('/api/patient/start',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({idNumber:id,gender,examType:exam})});
  const j=await res.json(); if(!j.ok){ alert(j.error||'خطأ'); return; } SESSION_ID=j.sessionId; document.getElementById('routeCard').style.display='block'; updateNotice(gender, exam); renderRoute(j.route, j.current); }
function updateNotice(gender, exam){ let msg=''; if(gender==='F'){ msg+=t('تنويه: يرجى التوجه إلى استقبال الطابق الثالث للتسجيل. ','Note: Please go to 3rd floor reception to register. ');} if(exam==='fitness'||exam==='recruit'){ msg+=t('المختبر/الأشعة في طابق الميزانين (M). باقي العيادات في الطابق الثاني.','Lab/Radiology on Mezzanine (M). Others on 2nd floor.'); } document.getElementById('notice').textContent=msg; speak(msg); }
function renderRoute(route, idx){ const box=document.getElementById('routeBox'); const steps=route.map((c,i)=>(i===idx?'➡️ ':'  ')+(LANG==='ar'?c.name_ar:c.name_en)+' ('+c.floor+')').join('\\n'); box.textContent=steps; const hint=t('أدخل رمز PIN من الممرضة/الطبيب للعيادة الحالية.','Enter clinic PIN from staff for the current step.'); document.getElementById('hint').textContent=hint; speak(t('تم تحديد المسار الطبي. يرجى الالتزام بالمسار.','Your medical route is set. Please follow the instructions.')); }
async function enterClinic(){ const pin=document.getElementById('pin').value.trim(); const res=await fetch('/api/visits/enter',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({sessionId:SESSION_ID,pin})}); const j=await res.json(); if(!j.ok){ alert(j.error||'PIN غير صحيح'); return; } if(j.done){ document.getElementById('doneCard').style.display='block'; document.getElementById('routeBox').textContent=t('اكتمل المسار.','Route complete.'); speak(t('تم إنهاء الفحص الطبي بنجاح.','Your exam is completed.')); } else { renderRoute(j.route, j.current); } }
